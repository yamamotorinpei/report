<!DOCTYPE html>
<html lang=ja>
<head>
<meta charset="UTF-8">
</head>
<body>
    <table>
        <% for( let result of content ) { %>
        <tr>
<td><%= result.id %></td>成績一覧
            <td>年度<%= result.year %></<td>
            <td>1対1<%= result.OV %></td>
            <td>2対2<%= result.TV %></td>
            <td>選手<%= result.player_name %></td>
            <td><%= result.taem_name %></td>
            <td>勝率<%= result.WR %></td>
            
        </tr>
        <% } %>
    </table>
    <form action="/" method="get">
        並び順：<select name="sorting">
            <option value=WR>勝率</option>
            <option value=OV>１対１</option>
            <option value=TV>２対２</option>
        </select>
        表示数：<input type="text" name="number" size="10">
        <input type="submit" value="並び替える">
    </form>
</body>
</html>
